console.log(
  "!!! `yarn dev` has been disabled. This build does not support HMR,",
);
console.log(
  "so you should use `yarn watch` and install the extension via your",
);
console.log("browser's dev tools");
