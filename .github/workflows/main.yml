name: Build with Make

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.x"
      - name: Install Yarn
        run: npm install -g yarn
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Run make
        run: make
      - name: Get Version number
        id: get-package-version
        run: node .github/version.cjs
      - run: mv dist/FIREFOX_anti-doomscroll_UNSIGNED.xpi dist/FIREFOX_anti-doomscroll_UNSIGNED-${{ steps.get-package-version.outputs.version }}-${{github.sha}}.xpi
      - run: mv dist/FIREFOX_anti-doomscroll.tar.gz dist/FIREFOX_anti-doomscroll-${{ steps.get-package-version.outputs.version }}-${{github.sha}}.tar.gz
      - run: mv dist-chrome/CHROME_anti-doomscroll_UNSIGNED.xpi dist-chrome/CHROME_anti-doomscroll_UNSIGNED-${{ steps.get-package-version.outputs.version }}-${{github.sha}}.xpi
      - run: mv dist-chrome/CHROME_anti-doomscroll.tar.gz dist-chrome/CHROME_anti-doomscroll-${{ steps.get-package-version.outputs.version }}-${{github.sha}}.tar.gz
      - name: Upload Firefox
        uses: actions/upload-artifact@v4
        with:
          name: FIREFOX_anti-doomscroll_UNSIGNED-${{ steps.get-package-version.outputs.version }}-${{github.sha}}.zip
          path: dist/FIREFOX_anti-doomscroll*
          if-no-files-found: error

      - name: Upload Chrome
        uses: actions/upload-artifact@v4
        with:
          name: CHROME_anti-doomscroll_UNSIGNED-${{ steps.get-package-version.outputs.version }}-${{github.sha}}.zip
          path: dist-chrome/CHROME_anti-doomscroll*
          if-no-files-found: error

  tests:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
      - name: Install Yarn
        run: npm install -g yarn
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Run tests
        run: yarn test
      - name: Run Lints
        run: yarn check
      - name: Run Checks
        run: yarn run check
      - name: Run make
        run: make
      - name: "web-ext lint"
        uses: kewisch/action-web-ext@v1
        with:
          cmd: lint
          source: dist
          channel: listed
